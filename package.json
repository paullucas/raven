{
  "name": "raven",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "lumo --init raven.cljs --quiet --repl"
  },
  "dependencies": {
    "osc-msg": "^0.4.0",
    "socket.io": "^2.0.1",
    "socket.io-client": "^2.0.2"
  }
}
